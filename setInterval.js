// setinterval is using to do something for us at a particular time whenever that interval comes it will go with the code that is written insdie the setinterval 

setInterval(() => {
    console.log("first")
}, 1000);