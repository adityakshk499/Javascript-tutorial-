// && and || operator 
// The logical AND operator (&&) returns true if both operands are true, and false otherwise. Here's an example:

// let a = true;
// let b = false;
// let result = a && b;
// console.log(result);

// The logical OR operator (||) returns true if at least one of the operands is true, and false otherwise. Here's an example:

// let a = true;
// let b = false;
// let result = a || b;
// console.log(result); // Output: true

// The logical AND (&&) and logical OR (||) operators are commonly used in conditional statements, validation, and error handling. For example:

// Conditional statement using logical AND
// if (age >= 18 && hasLicense) {
//     console.log("You can drive.");
// }

// // Validation using logical OR
// if (email || phoneNumber) {
//     console.log("Contact information provided.");
// }